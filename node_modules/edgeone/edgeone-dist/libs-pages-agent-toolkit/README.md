# pages-agent-toolkit wheel

此目录存放 `pages-agent-toolkit` 的 Python wheel 文件，随 CLI npm 包一起发货。

构建期通过 PEP 508 `pages-agent-toolkit @ file://...` 语法让 uv/pip 直接读本地文件，
绕开 PyPI index 解析（该包仅发布在内网 PyPI）。

由 `npm run prepare:agent-toolkit-wheel` 自动准备，不要手动修改。
